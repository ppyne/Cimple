void main() {
    int ts = makeEpoch(2025, 12, 31, 23, 59, 58);
    print(formatDate(ts, "%Y") + "\n");
    print(formatDate(ts, "%m") + "\n");
    print(formatDate(ts, "%d") + "\n");
    print(formatDate(ts, "%H:%M:%S") + "\n");
}
