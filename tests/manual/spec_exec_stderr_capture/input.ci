void main() {
    ExecResult r = exec(["/bin/sh", "-c", "printf 'oops' >&2; exit 3"], []);
    print(toString(execStatus(r)) + "\n");
    print("<" + execStdout(r) + ">\n");
    print("<" + execStderr(r) + ">\n");
}
