glyphAt normalises NFD input to NFC before indexing (spec §9.1).
"e\u0301lan" in NFD is 4 bytes for the first glyph; after NFC it becomes "élan",
so glyphAt(s, 0) returns the 2-byte NFC "é" and glyphLen returns 4.
