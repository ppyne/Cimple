void doubleAll(int[] arr) {
    for (int i = 0; i < count(arr); i++) {
        arr[i] = arr[i] * 2;
    }
}

void main() {
    int[] values = [1, 2, 3];
    doubleAll(values);
    print(toString(values[0]) + "," + toString(values[1]) + "," + toString(values[2]) + "\n");
}
