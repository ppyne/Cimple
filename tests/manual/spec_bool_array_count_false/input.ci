int countFalse(bool[] flags) {
    int total = 0;
    for (int i = 0; i < count(flags); i++) {
        if (!flags[i]) {
            total = total + 1;
        }
    }
    return total;
}

void main() {
    bool[] flags = [true, false, false, true];
    print(toString(countFalse(flags)) + "\n");
}
