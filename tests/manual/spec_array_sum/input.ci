int sumArray(int[] arr) {
    int total = 0;
    for (int i = 0; i < count(arr); i++) {
        total = total + arr[i];
    }
    return total;
}

int main() {
    int[] values = [1, 2, 3, 4, 5];
    print(toString(sumArray(values)) + "\n");
    return 0;
}
