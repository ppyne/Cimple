// finally completes normally — the return value from try is preserved
string helper() {
    try {
        return "try-result";
    } finally {
        print("finally ran\n");
    }
    return "";  // unreachable; satisfies semantic checker
}

void main() {
    string r = helper();
    print(r + "\n");
    print("done\n");
}
